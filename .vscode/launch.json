{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "启动风格挖图台",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app:app", "--host", "127.0.0.1", "--port", "8787", "--reload"],
      "jinja": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
